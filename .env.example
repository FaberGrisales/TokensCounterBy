# Copy this file to .env and fill in only the keys you actually plan to use.
# .env is already in .gitignore - it never gets committed.
# Requires python-dotenv (pip install python-dotenv) to be loaded automatically.

ANTHROPIC_API_KEY=
GEMINI_API_KEY=
HF_TOKEN=
OPENAI_API_KEY=
