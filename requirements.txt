# Only dependency: the TUI rendering library. This app makes no external
# API calls and needs no API keys - it only reads Claude Code's own local
# files (session transcripts, config), so nothing else is required.
rich
